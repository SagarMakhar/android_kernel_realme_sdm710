DEFS = -DCONFIG_OPPO_FINGERPRINT_PROJCT=18041
ccflags-y += $(DEFS)

DEFS_PLATFORM = -DCONFIG_OPPO_FINGERPRINT_PLATFORM=710
DEFS_IRQ = -DCONFIG_OPTICAL_IRQ_HANDLER
ccflags-y += $(DEFS_IRQ)

ccflags-y += $(DEFS_PLATFORM)

obj-y     +=  oppo_fp_common.o

